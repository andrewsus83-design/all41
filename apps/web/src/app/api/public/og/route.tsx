/**
 * Proxy-safe mirror of the root opengraph-image (src/app/opengraph-image.tsx).
 * The file-convention route (/opengraph-image) is not in proxy.ts PUBLIC yet; this one is.
 */
export { default as GET } from "@/app/opengraph-image";
